class DatabaseScreen {

    constructor({mac, title, type, id}){
        this.mac = mac;
        this.title = title;
        this.type = type;
        this.id = id;
    }

}

export default DatabaseScreen;